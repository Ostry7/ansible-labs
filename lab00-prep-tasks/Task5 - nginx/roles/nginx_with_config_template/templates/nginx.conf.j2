server {
    listen 80;
    server_name {{ server_name }};

    root {{ document_root }};
    index index.html;

    location / {
        return 200 "Nginx is working! Served from {{ document_root }}\n";
        add_header Content-Type text/plain;
    }
}
